const TTL = 100;
const B = 20; 
const C = 3; // Number of dimensions
const T = 10;
const INPUT_LAYER_SIZE = C * 5;
const STARTING_SCORE = 5;
const BOUNDS_SIZE = 30;

export { TTL, B, T, C, INPUT_LAYER_SIZE, STARTING_SCORE, BOUNDS_SIZE };